import { Meta } from '@storybook/blocks';

<Meta title="DesignPatterns/05-PrototypePattern" />

# Prototype Pattern

The prototype is an object that’s native to JavaScript, and can be accessed by objects through the prototype chain.

Note: Entire js is based on this one so no need to go in detail